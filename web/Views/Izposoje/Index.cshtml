@model IEnumerable<web.Models.Izposoja>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.DatumIzposoje)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DatumVrnitve)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Uporabnik)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.DatumIzposoje)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DatumVrnitve)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Uporabnik.Id)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IzposojaID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IzposojaID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IzposojaID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
